## demowebsite

Static portfolio site (HTML/CSS/JS).

### Run locally

```bash
python3 -m http.server 4173
```

Then open `http://127.0.0.1:4173`.

### Deploy

- Vercel: import the repo, set **Output Directory** to `.` (root), no build command.
- Netlify: deploy as a static folder.

